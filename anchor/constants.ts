export const IDL_VERSION = "v2";

export const AUTHORITY_SEED = `b_authority_${IDL_VERSION}`;

export const ORDER_BOOK_DETAIL_SEED = `b_order_detail_${IDL_VERSION}`;

export const ORDER_BOOK_WITH_TOKEN_SEED = `b_order_with_token_${IDL_VERSION}`;

export const ORDER_COUNTER_SEED = `b_order_counter_${IDL_VERSION}`;

export const ORDER_CONFIG_SEED = `b_order_config_${IDL_VERSION}`;

export const SOL_VAULT_SEED = `b_sol_vault_${IDL_VERSION}`;

export const SETTLE_POOL_SEED = `b_settle_pool_${IDL_VERSION}`;

export const MAX_ORDER_LIMIT = 100;
