export const IDL_VERSION = "v2";

export const AUTHORITY_SEED = `buymore_authority_${IDL_VERSION}`;

export const ORDER_BOOK_DETAIL_SEED = `buymore_order_detail_${IDL_VERSION}`;

export const ORDER_BOOK_WITH_TOKEN_SEED = `buymore_order_with_token_${IDL_VERSION}`;

export const ORDER_COUNTER_SEED = `buymore_order_counter_${IDL_VERSION}`;

export const ORDER_CONFIG_SEED = `buymore_order_config_${IDL_VERSION}`;

export const SOL_VAULT_SEED = `buymore_sol_vault_${IDL_VERSION}`;

export const SETTLE_POOL_SEED = `buymore_settle_pool_${IDL_VERSION}`;

export const MAX_ORDER_LIMIT = 100;
